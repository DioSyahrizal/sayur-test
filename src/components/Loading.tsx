import React from "react";
import { Box } from "../utils";

const Loading = () => {
  return (
    <Box
      display="flex"
      justifyContent="center"
      alignItems="center"
      minHeight="100vh"
    >
      Loading...
    </Box>
  );
};

export default Loading;
